name = "webhook-relay"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Environment variables (secrets will be set via wrangler secret put)
# [vars]
# Example: KEY = "value"

# KV Namespaces will be configured in later stories
